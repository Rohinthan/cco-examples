fn main() -> int {
    let x = 10;
    let y = 9;

    let is_greater = x > y;

    print(f"Values: x = {x}, y = {y}");
    print(f"Stored comparison result (is_greater): {is_greater}");
    return 0;
}
