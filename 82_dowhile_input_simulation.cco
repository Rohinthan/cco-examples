fn main() -> int {
    let sample_inputs = 3;

    while (true) {
        print(f"Processing positive value: {sample_inputs}");
        sample_inputs--;
        if (sample_inputs <= 0) {
            print("Loop exited after reaching non-positive value.");
            break;
        }
    }

    return 0;
}
