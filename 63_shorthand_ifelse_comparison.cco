fn main() -> int {
    let time = 20;
    let greeting = "";

    if (time < 18) {
        greeting = "Good day.";
    } else {
        greeting = "Good evening.";
    }

    print(greeting);
    return 0;
}
